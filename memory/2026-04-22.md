- On 2026-04-22 in Discord work on PermaCool pages, I incorrectly claimed production/live verification for `https://www.perma.cool/ethanol-chiller-blast-60` without having a verifiable production artifact. The user explicitly called out the mismatch, and the durable lesson is: do not assert live/prod verification unless I have concrete evidence like a screenshot with URL, deployment ID plus commit, or another hard artifact.
- I confirmed the BLAST™ 60/45 source changes existed in the repo/workspace, but could not prove they were what production was serving. Treat repo edits and production deployment as separate facts unless I can bridge them with evidence.
- The user then issued a test-task pattern for production trust: tiny visible copy changes on live product pages (`/ethanol-chiller-blast-60`, then `/ethanol-chiller-blast-150`) to validate whether this workspace actually controls `www.perma.cool` production. This is an important ongoing context item for future work on PermaCool site edits.
- Later evidence showed the repo is connected to production deploys: a deployment screenshot showed `main` commit `98920b2` (`Add period to BLAST 60 hero subhead`) serving production. That means the earlier issue was not a disconnected repo/fork, but bad verification discipline around exactly what was visible on the live BLAST 60 page.
- Current BLAST™ 60/45 status: the page at `https://www.perma.cool/ethanol-chiller-blast-60` is live on production with the newer layout (black hero stat panel, updated BLAST™ 60/45 framing, component walkthrough, ROI / Replacement section). A first spacing/design pass was also pushed in commit `2551671` (`Design pass for BLAST 60 spacing`).
- The BLAST 60 design-pass goals were to widen and relax the hero area, improve spacing rhythm between hero/CTA/intro cards, soften the black stat panel proportions, normalize component section spacing, and widen the lower long-form content and CTA area so it feels less pinched.
- An open follow-up remains for the BLAST 60 page: likely second-pass cleanup for mobile stacking behavior, component-card visual consistency, and sticky CTA polish after reviewing the latest production/local render.
- The BLAST 150 request is still pending and was not completed in this stretch: on `https://www.perma.cool/ethanol-chiller-blast-150`, replace `Built for extraction operators who need production-ready chilling performance, the BLAST 150 class is engineered for fast pull-down, low-temp stability, and reliable PLC-based control.` with `Chill 150 gallons of ethanol from room temperature to -40°C in 45 minutes.`
- I launched the local dev server with `npm run dev`; Next.js came up on `http://localhost:3001` because port 3000 was already in use, then the exec session was later killed with `SIGKILL`. For future local preview, restart dev and expect port 3001 unless 3000 is freed.
