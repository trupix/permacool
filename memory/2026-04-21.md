- Continued iterative edits on `app/ethanol-chiller-blast-60/page.jsx` for the BLAST™ 60/45 page in Discord-driven review.
- User wanted the component overview section fully rebalanced for image sizing, alignment, and spacing relative to text blocks.
- Repeatedly increased the `blast-60-assets-overview.jpg` image size after user feedback that prior changes were not visually noticeable because the grid column was constraining it.
- Important lesson: on this page, changing image `max-width` alone may not visibly affect layout; the parent grid column width also needs to be widened.
- User replaced the lower cascade section from an uploaded image, then asked to remove a specific bullet-style line list from that section.
- Latest user preference for the lower sections: keep them visually contained in a box instead of running full width across the page.
- The BLAST 60 page continues to be brittle because `page.jsx` stores the main content in one large escaped `mainHtml` string, making partial exact-match edits unreliable and full-file rewrites more practical.
