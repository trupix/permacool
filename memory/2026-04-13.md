# 2026-04-13

- Continued iterative editing of the PermaCool ethanol chilling systems page with live-check workflow: edit repo, commit only intended files, push to `main`, then verify on Vercel.
- Confirmed repo remote/branch workflow for live updates: `https://github.com/trupix/permacool.git` on `main` with Vercel auto-deploy.
- User preferred this workflow over local preview because they can see changes directly on the live beta.
- Important security finding: `.env.local` in workspace contained a live-looking Vercel OIDC token and should be treated as compromised/rotated if still valid.
- Main page actively edited: `app/ethanol-chilling-systems/page.jsx` plus related product/hero images under `public/assets/images/`.
- Established user style preferences on this page:
  - keep main title centered
  - keep eyebrow text left-aligned
  - use dedicated hero-image styles rather than inline sizing where possible
  - prefer simpler/plain image presentation first, then iterate visually
  - liked current badge + cleaner headline treatment for the ON-DEMAND COLD ETHANOL callout
- Latest confirmed layout state on the BLAST 150/45 card: reverted to stacked layout with the image restored to prior size on top and the BLAST 150/45 text block underneath.
