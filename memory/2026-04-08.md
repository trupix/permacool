- PermaCool site now runs as a Next.js app in this workspace; important routes/pages actively edited include `app/page.jsx`, `app/insights/page.jsx`, and `app/workflow/page.jsx`.
- Recent PermaCool website changes completed and pushed: added Workflow page, inserted workflow diagram + full body copy, tuned the 8 step icons, added homepage link from the process card to `/workflow`, and renamed homepage step text to `03 • Rinse & Repeat`.
- Relevant recent commits for PermaCool continuity: `45238a2` (workflow placeholder page), `5c6a36c` (workflow diagram image), `0eac67f` (workflow body copy), `dfd2f58` (icons on workflow steps), `ee7e079` (refined workflow icons), `b876241` (homepage workflow link), `ba6b8ca` (rename to Rinse & Repeat).
- Important lesson from Vercel incident: partial Next.js pushes caused homepage/root deployment breakage; safest pattern is to make scoped edits inside the current app structure and ensure required `app/` routes are committed before pushing.
- User prefers article/content handoff in clean structured text rather than Google-exported HTML: title, summary, section headings, paragraphs/bullets, CTA, and separate image attachments.
